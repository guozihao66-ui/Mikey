Build a working local prototype web app in the current directory for an "Okeanos Marketing AI Team Platform".

Non-negotiable constraints:
- ALL UI text and deliverables must be in ENGLISH.
- Do NOT ask clarifying questions.
- Make reasonable assumptions and document them in a NOTES.md file.
- Build actual runnable code, not just docs.
- The prototype must support basic interaction with an "AI Team Leader" interface.
- Brand direction: engineer credibility + high quality + practical value + low-friction lead generation.

Company context to assume:
- Okeanos Pools GTA / Okeanos Ontario
- Ontario-based fiberglass pool company
- Website: https://www.okeanospoolsgta.com/
- This platform is for the internal marketing team
- Priority goals:
  1. stronger social/reputation
  2. marketing department operating system
  3. faster lead response
  4. more website leads
- Automation model: AI drafts and recommends, humans approve before execution

Goal:
Create a prototype platform that a user can open locally in the browser and interact with.

Required MVP features:
1. A dashboard/home page for the Okeanos Marketing AI Team.
2. A visible AI Team structure with at least these agents:
   - AI Team Leader
   - Social & Reputation Agent
   - Content Strategist Agent
   - Lead Response Agent
   - Reporting Agent
3. A chat-style interface where the user can type requests to the AI Team Leader.
4. The Team Leader should support basic prototype interactions such as:
   - clarifying a requirement
   - generating a work summary
   - generating a weekly marketing report
   - routing a task to another agent in the UI
5. A reports area containing:
   - sample weekly marketing report output
   - sample work summary output
6. A task/activity panel showing routed tasks and statuses.
7. Basic but polished UI suitable for an internal marketing team prototype.
8. Seed the app with Okeanos-specific positioning and examples.

Important implementation guidance:
- This is a prototype, so you may mock AI behavior locally.
- Use a simple local stack that can run easily, preferably Vite + React.
- If using a backend, keep it minimal and easy to run.
- Include clear run instructions in README.md.
- Include a simple prompt/response simulation layer so the Team Leader feels interactive.
- The Team Leader should respond differently for at least these request types:
  - weekly report
  - work summary
  - social/reputation task
  - lead response task
  - general clarification
- Include a few preloaded task cards and report examples.
- Use sensible file organization.
- Add comments where helpful.
- Make the design feel credible and professional, not flashy.

Deliverables to create:
- runnable app source code
- README.md with setup/run instructions
- NOTES.md with assumptions and next steps
- sample data files if useful

After building:
1. install dependencies if needed
2. run a production build or equivalent validation
3. commit changes to git; if git identity is missing, configure local repo only with:
   - user.name = OpenClaw
   - user.email = openclaw@local
4. print a concise completion summary including how to run the prototype

When completely finished, run this command to notify me:
openclaw system event --text "Done: Built Okeanos marketing AI team prototype" --mode now
